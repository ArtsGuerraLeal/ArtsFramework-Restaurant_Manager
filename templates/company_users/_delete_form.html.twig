<form method="post" action="{{ path('company_users_delete', {'id': company_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company_user.id) }}">
    <button class="btn">Delete</button>
</form>
