{% extends 'base.html.twig' %}

{% block title %}CustomData{% endblock %}

{% block body %}
    <h1>CustomData</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ custom_datum.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ custom_datum.name }}</td>
            </tr>
            <tr>
                <th>IsNum</th>
                <td>{{ custom_datum.isNum ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('custom_data_index') }}">back to list</a>

    <a href="{{ path('custom_data_edit', {'id': custom_datum.id}) }}">edit</a>

    {{ include('custom_data/_delete_form.html.twig') }}
{% endblock %}
