{% extends 'base.html.twig' %}

{% block title %}CustomForm{% endblock %}

{% block body %}
    <h1>CustomForm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ custom_form.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ custom_form.name }}</td>
            </tr>
            <tr>
                <th>Fields</th>
                <td>{{ custom_form.fields ? custom_form.fields|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('custom_form_index') }}">back to list</a>

    <a href="{{ path('custom_form_edit', {'id': custom_form.id}) }}">edit</a>

    {{ include('custom_form/_delete_form.html.twig') }}
{% endblock %}
