{% extends 'base.html.twig' %}

{% block title %}MaritalStatus{% endblock %}

{% block body %}
    <h1>MaritalStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marital_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ marital_status.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ marital_status.createdAt ? marital_status.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ marital_status.updatedAt ? marital_status.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('marital_status_index') }}">back to list</a>

    <a href="{{ path('marital_status_edit', {'id': marital_status.id}) }}">edit</a>

    {{ include('marital_status/_delete_form.html.twig') }}
{% endblock %}
